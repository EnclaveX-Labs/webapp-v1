# EnclaveX Labs — Website

Minimal Next.js scaffold for the EnclaveX Labs website.

Run locally:

```bash
npm install
npm run dev
```

This seed includes:
- pages: `index.js`, `_app.js`
- components: `Header`, `Footer`, `Hero`, `Section`, `Layout`
- global styles: `styles/globals.css`

Next steps: refine copy, add images, and match exact typography and spacing from the reference design.
